struct Foo {
	bar (u32, u32),
	baz [u32],

	func new() Foo {}
}

// comment
func main() bool {
	var a u32 = 0;
	var b [u32] = [1, 2];
	var c (u32, u32, [u32]) = (1, 2, [1, 2, 3]);
	var foo Foo = undefined;
	var d u32 = c.1;
	var x (u32, u32) = foo.bar;
	if true {
		return 4.20 == 6.9;
	};
	if 1 == 2 0 else {
		return 1 == 2;
	};

	foo(1, 2,);

	return 1 + 2 * 3;
}

func foo(foo u32, bar u32,) void {
	return foo + bar;
}
